<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.heartcarelite">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HeartCareLite">
        <activity
            android:name=".ui.CvdRiskChart.CvdRiskChartActivity"
            android:exported="true" />
        <activity
            android:name=".ui.userDetails.UserDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".ui.userList.UserListActivity"
            android:exported="true" />
        <activity
            android:name=".ui.cvdRisk.saveRecord.SaveRecordActivity"
            android:exported="true" />
        <activity
            android:name=".ui.cvdRisk.CvdRiskActivity"
            android:exported="true" />
        <activity
            android:name=".ui.landingPage.LandingActivity"
            android:exported="true" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>